<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neuedu.dao.CartMapper" >
  <resultMap id="BaseResultMap" type="com.neuedu.pojo.Cart" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="product_id" property="productId" jdbcType="INTEGER" />
    <result column="quantity" property="quantity" jdbcType="INTEGER" />
    <result column="checked" property="checked" jdbcType="INTEGER" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    delete from neuedu_cart
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.neuedu.pojo.Cart" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into neuedu_cart (id, user_id, product_id, 
      quantity, checked, create_time, 
      update_time)
    values (#{id,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER}, #{productId,jdbcType=INTEGER}, 
      #{quantity,jdbcType=INTEGER}, #{checked,jdbcType=INTEGER}, now(), now())
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.neuedu.pojo.Cart" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    update neuedu_cart
    set user_id = #{userId,jdbcType=INTEGER},
      product_id = #{productId,jdbcType=INTEGER},
      quantity = #{quantity,jdbcType=INTEGER},
      checked = #{checked,jdbcType=INTEGER},
      update_time = now()
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, user_id, product_id, quantity, checked, create_time, update_time
    from neuedu_cart
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, user_id, product_id, quantity, checked, create_time, update_time
    from neuedu_cart
  </select>


  <!--
      根据userid和productid查询购物信息
    -->

  <select id="selectCarByuserIdAndproductId" resultMap="BaseResultMap" parameterType="map">
    select id, user_id, product_id, quantity, checked, create_time, update_time  from neuedu_cart
    where user_id=#{userId}  and  product_id=#{productId}
  </select>

  <!--
      根据userid查询购物信息
    -->

  <select id="selectCarByuserId" resultMap="BaseResultMap" parameterType="int">
    select id, user_id, product_id, quantity, checked, create_time, update_time  from neuedu_cart
    where user_id=#{userId}
  </select>

  <!--
      统计用户购物信息是否全选
    -->
  <select id="ischeckedAll" parameterType="int" resultType="int">
      select count(1) from neuedu_cart where user_id=#{userId} and checked = 0
  </select>

  <!--
      删除购物车中某些商品
    -->
  <delete id="deleteproByuserIdAndproductId" parameterType="map">
    delete
    from neuedu_cart
    <where>
      user_id=#{userId}
      <if test="productIdList!=null and productIdList.size>0">
        and product_id
        in
        <foreach collection="productIdList" item="productId" open="(" close=")" separator=",">
          #{productId}
        </foreach>
      </if>
    </where>
  </delete>


  <!--
      操作购物车是否选中
    -->
  <update id="selectOrUnselectByproductId" parameterType="map">
      update neuedu_cart set checked=#{check}
      <where>
        user_id=#{userId}
        <if test="productId !=null">
          and product_id=#{productId}
        </if>
      </where>
  </update>


  <!--
     统计购物车中商品数量
    -->
  <select id="get_cart_product_count" resultType="int" parameterType="int">
        select ifnull(sum(quantity),0) from neuedu_cart where user_id=#{userId}
  </select>

  <!--
     查询购物车中用户选中的商品
    -->
  <select id="findcartListByuserIdAndchecked" resultMap="BaseResultMap" parameterType="int">
        select id, user_id, product_id, quantity, checked, create_time, update_time  from neuedu_cart
    where user_id=#{userId} and checked=1
  </select>

  <!--
       清空购物车
      -->
  <delete id="batchDelete"  parameterType="list">
       delete from neuedu_cart
       <where>
         id  in
         <foreach collection="list" item="cart" open="(" close=")" separator=",">
           #{cart.id}
         </foreach>
       </where>
  </delete>
</mapper>